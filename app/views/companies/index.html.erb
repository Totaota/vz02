    <div class="row">
          <div class="col-md-3">
                <ul class="sidebar-list">
                       <li class="sidebar-item"><%= link_to "Mon entreprise", companies_path, class: "sidebar-link active" %></li>
                </ul>
           </div>
           <div class="col-md-9">
                <div class="panel panel-default">
                     <div class="panel-heading">Mon entreprise
                     </div>
                    <div class="panel-body">
                          <% @company.each do |company| %>
                             <div class="row">
                                     <div class="col-md-2">
                                          <%= image_tag company.photos[0].image.url(:thumb) if company.photos.length > 0 %> 
                                     </div>                              
                                     <div class="col-md-7">
                                            <h4><%= company.social_reason %></h4>
                                     </div>
                                     <div class="col-md-3 right">
                                         <%= link_to "Mettre à jour", edit_company_path(company), class: "btn btn-primary" %>
                                     </div>
                            </div>
                       <% end %>
                   </div>
               </div>
         </div>
     </div>